import { Component } from './Component';

/**
 * Tag component to identify an entity as an enemy.
 * Used by the collision system to detect stomp/side interactions.
 */
export class EnemyTag extends Component {}
